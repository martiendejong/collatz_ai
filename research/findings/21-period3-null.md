# F21 — Period-3 Census: empty. The integer cycle list is complete in range.

**Status: PROVEN (exhaustive, 10^6 shapes, kᵢ, wᵢ ≤ 10, sign-free).**

Zero non-repeat period-3 integer realizations. Combined with F19 (periods 1–2) and the known
classification of cycles on ℤ (1, −1, −5, −17 — the −1 cycle sits at the coordinate boundary),
the census has now:
- found every cycle that exists (validation),
- found nothing else at periods 1, 2, 3 (completeness in range),
- confirmed the Sign Theorem's prediction pattern: the positive side is empty beyond trivial.

The census program is now mechanical: period-m completeness is a finite computation for any bound.
The remaining cycle risk lives only at (large period) × (large shape) × (Baker-throttled coincidence).

Related: [[19-realization-census]], [[20-sign-theorem-catalan]]
