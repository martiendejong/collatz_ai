# F35 — Campaign III (Rounds 124–173)

## The homogenization flagship: cascade confirmed by three instruments
- **R124–126 (profile):** CV by trit-position inside each certificate: heterogeneity enters at
  the mod-9 source (CV ≈ 0.43–0.45) and decays geometrically up the positions with bulk factor
  **θ ≈ 0.84** (~40 ratios, all ≤ 0.87; consistent across k = 13/15/17; top-position values
  reproduce the endpoint CVs exactly).
- **R127–130 (spectrum):** full eigendecomposition of the edge-linearized operator at k = 5/6/7:
  ONE clean spectral gap (1 → 0.75/0.84/0.82) then a dense band — the subleading modulus IS the
  cascade factor.
- Together with the endpoint decay across k: three independent instruments agree on a stable
  contraction ≈ 0.84 < 1. The Homogenization Conjecture (Theorem 15c's hypothesis) is now
  supported by a profile, a law, and a spectrum. Proving a uniform gap remains THE open target;
  the operator's explicit structure (Lemmas A/B: geometric sums over ×4-orbits composed with
  branch bijections and 1-Lipschitz minima) is the attack surface.

## Sibling-system exponents (first measurements anywhere)
- **3x−1:** the three trees are all FAT — exponents 0.899 / 0.897 / 0.902 (roots 1, 5, 17),
  jointly 59.3% of integers below 10⁷ (count exponent 0.967): a three-attractor world where
  everything grounds, split roughly evenly.
- **5x+1:** grounded trees are THIN SKELETONS — exponents 0.547 (tree 1), 0.586 (tree 13),
  0.492 (tree 17): in a genuinely divergent world the grounded exponent sits near 0.55.
  **The Collatz bound ≥ 0.9069 vs the divergent-world ~0.55: grounded and floating universes
  differ measurably in exponent class.**

## Anatomy of the 547-ride (12,235,060,455)
126 macro-steps = 547 T-steps. Strategy: **win early, coast long** — k-mean 2.74 (fair 2.0),
w-mean 1.60 (fair 2.0); +11 bits climbed in the first 33 macro-steps (peak ×7,700), then 93
steps of fair decay spending the cushion to −0.19 bits at the fatal step; only 3 near-death
moments. Deep records ≈ time-to-peak + peak/drift: descent time from an early peak, not
sustained luck — the tail law's t^(−3/2)·2^(−t/20) anatomy, observed in the flesh.

## Closures and partials
- **Base-3/2 grammar (R131–134): PARTIAL.** The insertion law is conditional — exact on orbit
  values, ~33% on arbitrary odds; even steps are not single edits. Full grammar deferred to the
  AFS carry conventions.
- **Spelling-language route (R120): closed** — the machine forgets (2999× compression).
- Blocker-lattice (conditional per-prime congruences): parked as rigorous future work.

## Publication package
- `NOTE_DENSITY.tex` drafted (LaTeX, submission format): abstract, system, method, certificate
  table, verification section, min-loss identity + ceiling discussion, k=21 remark, references.
- `VERIFICATION.md`: the six-level anti-hallucination protocol.
- Certificates + chunked exact verifier: complete and re-run clean.
